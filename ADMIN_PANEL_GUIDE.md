# 管理画面ガイド

## 概要

FeliCa Gate Systemの管理画面が大幅にアップデートされました。新しい管理画面では、以下の機能が利用できます：

- ユーザー管理（追加、残高編集、削除）
- 定期券管理（追加、無効化、削除）
- 入退場記録管理（出場登録、キャンセル）
- カード一覧表示
- 駅・ゲート一覧表示

## アクセス方法

1. サーバーを起動:
```bash
cd server
source .venv/bin/activate
python main.py
```

2. ブラウザで管理画面を開く:
```
file:///Users/yuki/Developer/Felica_Gate_System/admin/index.html
```

または、任意のHTTPサーバーで `admin/` ディレクトリをサーブしてください。

## 機能一覧

### 1. ユーザー管理

#### 表示項目
- ユーザーID
- 名前
- メールアドレス
- 残高（¥1,000未満は赤色バッジで警告表示）
- QRトークン

#### 操作
- **➕ ユーザー追加**: 新しいユーザーを作成
  - 名前（必須）
  - メールアドレス
  - 初期残高（デフォルト: ¥10,000）
  - QRトークン

- **残高編集**: ユーザーの残高を直接変更

- **削除**: ユーザーを削除（関連する定期券・トリップも削除されます）

### 2. 定期券管理

#### 表示項目
- 定期券ID
- ユーザーID
- 種別（通勤定期 / 通学定期）
- 区間（駅コード間）
- 有効期間
- 状態（有効 / 無効）

#### フィルター
- **有効な定期券のみ表示**: チェックを入れると、期限内かつアクティブな定期券のみ表示

#### 操作
- **➕ 定期券追加**: 新しい定期券を作成
  - ユーザー選択（必須）
  - 種別（通勤定期 / 通学定期）
  - 開始駅（必須）
  - 終了駅（必須）
  - 有効期間開始（必須、デフォルト: 今日）
  - 有効期間終了（必須、デフォルト: 3ヶ月後）

- **無効化**: 定期券を無効化（`is_active = 0`に設定）

- **削除**: 定期券を完全に削除

### 3. 入退場記録（Trips）管理

#### 表示項目
- トリップID
- ユーザーID
- 入場情報（駅、ゲート、日時）
- 出場情報（駅、ゲート、日時）
- ステータス（入場中 / 完了 / キャンセル）
- 使用した定期券ID

#### フィルター
- **ステータスフィルター**: 全て / 入場中 / 完了 / キャンセル

#### 操作
- **➕ 記録追加**: 新しい入場記録を作成
  - ユーザー選択
  - 入場駅
  - 入場ゲート
  - 入場日時
  - ※現在、この機能は `/scan` エンドポイント経由での実装が必要です

- **出場登録**: 入場中のトリップに出場情報を追加
  - 出場駅
  - 出場ゲート
  - 出場日時
  - ※現在、この機能は `/scan` エンドポイント経由での実装が必要です

- **キャンセル**: トリップをキャンセル状態に変更

### 4. カード管理

#### 表示項目
- カードID
- ユーザーID
- 種別（QR / FeliCa）
- 識別子（QRトークン or IDm）
- ラベル

※現在、カードの追加・削除機能は実装されていません

### 5. 駅・ゲート管理

#### 駅一覧
- 駅ID
- 駅コード
- 駅名

#### ゲート一覧
- ゲートID
- ゲートコード
- 駅ID
- ゲート名称

※現在、駅・ゲートの追加・編集・削除機能は実装されていません

## デザイン

新しい管理画面は以下の特徴を持ちます：

- **モダンなカードベースレイアウト**: 各セクションがカードとして表示
- **テーブルベースの表示**: データが見やすい表形式で表示
- **カラーコード済みバッジ**: 状態や種別が一目で分かるバッジ表示
- **モーダルダイアログ**: フォームが美しいモーダルで表示
- **レスポンシブデザイン**: モバイルでも使いやすいレイアウト
- **ホバーエフェクト**: インタラクティブなUI体験

## カラースキーム

- **成功（緑）**: 有効な状態、完了した操作
- **警告（オレンジ）**: 注意が必要な状態
- **危険（赤）**: 削除、エラー、残高不足
- **情報（青）**: 進行中の状態
- **セカンダリ（グレー）**: 無効な状態、キャンセル

## ボタンの種類

- **btn-primary（青）**: 主要な操作（全て更新など）
- **btn-success（緑）**: 新規作成・追加
- **btn-secondary（グレー）**: 更新・リフレッシュ、キャンセル
- **btn-warning（オレンジ）**: 編集・無効化
- **btn-danger（赤）**: 削除

## API エンドポイント

管理画面が使用するAPIエンドポイント：

### ユーザー
- `GET /users` - ユーザー一覧取得
- `POST /users` - ユーザー作成
- `PATCH /users/{user_id}/balance` - 残高更新
- `DELETE /users/{user_id}` - ユーザー削除

### 定期券
- `GET /passes` - 定期券一覧取得
- `POST /passes` - 定期券作成
- `PATCH /passes/{pass_id}/deactivate` - 定期券無効化
- `DELETE /passes/{pass_id}` - 定期券削除

### トリップ
- `GET /trips` - トリップ一覧取得
- `PATCH /trips/{trip_id}/cancel` - トリップキャンセル

### その他
- `GET /cards` - カード一覧取得
- `GET /stations` - 駅一覧取得
- `GET /gates` - ゲート一覧取得

## トラブルシューティング

### CORS エラーが出る場合

ブラウザのコンソールに CORS エラーが表示される場合、サーバー側で CORS が正しく設定されているか確認してください。

`server/main.py` に以下の設定があることを確認：

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### データが表示されない場合

1. サーバーが起動しているか確認:
```bash
curl http://localhost:8000/users
```

2. ブラウザの開発者ツール（F12）でネットワークタブを開き、エラーがないか確認

3. `admin/api.js` の `API_ROOT` がサーバーのアドレスと一致しているか確認

### モーダルが表示されない場合

ブラウザのコンソールにJavaScriptエラーがないか確認してください。

## 今後の拡張予定

- トリップの直接追加・編集機能の実装
- カードの追加・削除機能
- 駅・ゲートの管理機能
- 統計ダッシュボード
- CSVエクスポート機能
- リアルタイム更新（WebSocket）

---

## 技術スタック

- **HTML5**: セマンティックなマークアップ
- **CSS3**: フレックスボックス、グリッドレイアウト、アニメーション
- **JavaScript (ES6+)**: Async/Await、モジュール、イベントハンドリング
- **Fetch API**: REST API通信
- **FastAPI**: バックエンドサーバー

## ファイル構成

```
admin/
├── index.html      # メインHTML（構造）
├── styles.css      # スタイルシート（デザイン）
├── api.js          # JavaScript（機能・API通信）
└── README.md       # 管理画面の説明
```

---

**最終更新**: 2025年12月14日
